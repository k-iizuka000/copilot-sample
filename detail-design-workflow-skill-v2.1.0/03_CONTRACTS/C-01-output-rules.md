# C-01 出力共通ルール

1. 不明値は`null`。空文字や「不明」で代用しない。
2. 配列は常に配列。0件は`[]`。
3. Enumは契約で定義した値だけを使う。
4. JSONは純粋なJSONとし、コードフェンスや説明文を混ぜない。
5. `additionalProperties: false`を守る。
6. IDと並び順を安定させ、同一入力では原則同一出力にする。
7. 推測値を確定値として保存しない。
8. 指定出力先以外を変更しない。
