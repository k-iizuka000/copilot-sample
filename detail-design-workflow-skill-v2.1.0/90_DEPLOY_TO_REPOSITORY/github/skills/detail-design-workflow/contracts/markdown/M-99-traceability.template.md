---
contractId: M-99
schemaVersion: "1.0.0"
featureId: "{{featureId}}"
featureName: "{{featureName}}"
generationRunId: "{{runId}}"
generationStatus: "{{status}}"
evaluationStatus: "{{evaluationStatus}}"
humanReviewStatus: not_reviewed
sourceRecordCount: {{sourceRecordCount}}
openIssueCount: {{openIssueCount}}
---

# トレーサビリティ

## 1. JSON Record → Markdown

| Record ID | Record Type | JSONファイル | Markdownファイル | 見出し/表行 | 出典 |
|---|---|---|---|---|---|

## 2. sourceUnitカバレッジ

| Source Unit ID | disposition | Record IDs | 理由 |
|---|---|---|---|

## 3. 件数照合

```text
sourceUnitCount = accountedSourceUnitCount
unaccountedSourceUnitCount = 0
JSON record count = Markdown trace record count
```
