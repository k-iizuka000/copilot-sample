# AGENTS.md

このファイルは、このリポジトリで作業する AI コーディングエージェント向けの常時適用ルールです。


## プロジェクトルール
- 本リポジトリはpublicリポジトリです。
- そのため絶対パスやユーザーの個人情報をコミット対象にすることは禁止です

### セキュリティチェック
- 作業完了時に絶対パスやユーザーの個人情報がソースに残っていないかチェックしてください


目的は、短期的な修正ではなく、保守性・安全性・検証可能性を高めながらリポジトリを成長させることです。

## 最優先方針

- 既存の設計、命名、ディレクトリ構成、テスト方針を尊重する。
- 変更は小さく、可逆的で、レビューしやすい単位に保つ。
- ユーザーの明示指示を最優先する。
- 複数の指示がある場合は、より具体的で、より近いディレクトリにある指示を優先する。
- 不明点を推測で埋めない。根拠となるファイルを確認し、判断理由を残す。
- 依存関係、公開 API、データベーススキーマ、認証・認可、CI/CD、ライセンス、セキュリティ設定を変更する場合は、必要性と影響を明示する。

## 作業開始時に確認するもの

- `README*`
- `CONTRIBUTING*`
- `docs/`
- `package.json`
- `pnpm-lock.yaml`
- `yarn.lock`
- `package-lock.json`
- `pyproject.toml`
- `requirements*.txt`
- `go.mod`
- `Cargo.toml`
- `pom.xml`
- `build.gradle*`
- `Makefile`
- `Taskfile*`
- `Dockerfile`
- `compose*.yml`
- lint、format、typecheck、test の設定ファイル
- `.github/workflows/`
- 既存実装と既存テスト

新しい方針を作る前に、既存パターンを確認して従う。

## コマンド方針

- コマンドは推測せず、リポジトリ内のスクリプト、CI、README、設定ファイルから特定する。
- パッケージマネージャはロックファイルから判断する。
- 代表的な検証順序は以下とする。ただし、リポジトリ固有の手順がある場合はそれを優先する。

1. 依存関係の整合性確認
2. format
3. lint
4. typecheck または compile
5. unit test
6. integration test または e2e test
7. build

- 実行できなかったコマンドは、失敗理由、実行環境、代替検証を明記する。
- 信頼できるセットアップ手順や検証コマンドを発見した場合は、将来の作業者のために `AGENTS.md`、README、または関連ドキュメントへ簡潔に追記する。

## 実装ルール

- 既存コードのスタイル、命名、抽象化レベル、エラー処理、テスト構造に合わせる。
- 最小限の変更で目的を達成する。
- 関係ないリファクタリングを混ぜない。
- 可読性を優先する。
- 過度に巧妙な実装、不要な抽象化、早すぎる最適化を避ける。
- 型が使える環境では型安全性を高める。
- `any`、型アサーション、null 回避の乱用を避ける。
- 入出力境界では、バリデーション、エラー処理、ログ、監査可能性を考慮する。
- 同じロジックを重複させず、既存ユーティリティを確認してから追加する。
- public API、CLI、設定ファイル、データ構造を変更した場合は、互換性と移行方法を明記する。

## テスト方針

- 変更した挙動には、既存スタイルに沿ったテストを追加または更新する。
- バグ修正では、可能な限り再発防止テストを追加する。
- 正常系だけでなく、境界値、異常系、権限、空入力、並行性、外部依存の失敗を検討する。
- スナップショット更新は意図を確認し、不要な大規模更新を避ける。
- テストできない変更は、その理由と手動検証手順を残す。

## セキュリティと安全性

- 秘密情報、トークン、鍵、認証情報をコード、ログ、テストデータ、コミットに含めない。
- `.env`、key files、credentials、production config、user data を不用意に読み書き・出力しない。
- 外部入力は信頼しない。
- インジェクション、XSS、SSRF、パストラバーサル、認可漏れを確認する。
- SQL、コマンド、テンプレートは安全な API を使い、文字列連結で組み立てない。
- 依存関係追加は最小限にし、保守性、ライセンス、既知脆弱性、バンドルサイズを考慮する。

## ドキュメント方針

- 挙動、設定、API、コマンド、制約が変わる場合は関連ドキュメントを更新する。
- README は人間向けの概要として保つ。
- `AGENTS.md` には AI エージェントが作業するための実務情報を書く。
- 将来の作業を楽にする発見は、短く具体的に記録する。
- コメントは「何を」より「なぜ」を説明する。
- 自明なコードにコメントを増やさない。

## リポジトリ成長ルール

- その場限りの修正ではなく、再発防止、テスト追加、設計の明確化、ドキュメント改善を優先する。
- 複雑な変更は、調査、設計、実装、検証、文書化に分ける。
- 技術的負債を見つけた場合は、タスクに直接関係する範囲だけ改善する。
- タスク外の技術的負債は、隠さずに残課題として記録する。
- 既存の問題を隠すために、テストを弱めたり、エラーを握りつぶしたりしない。

## 触ってはいけない、または注意する領域

- `node_modules/`
- `vendor/`
- generated files
- build artifacts
- lockfiles
- migration
- database schema
- production config
- CI/CD
- 認証・認可
- 課金
- データ削除処理

これらは必要性が明確な場合だけ変更する。

## Git と作業履歴

- 明示的に求められない限り、Git 履歴を書き換えない。
- 明示的に求められない限り、勝手に commit、push、force push しない。
- 大規模なフォーマット変更、ファイル移動、リネームは、タスクに必要な場合だけ行う。
- 変更理由、影響範囲、検証結果を説明できる状態にする。

## 完了条件

作業完了前に、以下を確認する。

- 変更内容がユーザー依頼と一致している。
- 既存の設計・命名・テスト方針に沿っている。
- 関連する format、lint、typecheck、test、build を実行した。
- 実行できない検証がある場合は、理由と代替検証を説明した。
- 新しい失敗や警告を無視していない。
- ドキュメント更新が必要な場合は更新した。
- 残課題、前提、未検証事項を隠していない。