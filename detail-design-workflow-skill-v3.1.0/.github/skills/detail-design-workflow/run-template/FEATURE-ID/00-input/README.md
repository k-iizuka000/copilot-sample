# 00-input

このフォルダに、変換したい設計書のスナップショット（CSV / TSV など）を置きます。
Excel をそのまま Copilot が読めない場合は、シートを CSV / TSV に書き出してここへ保存してください（この書き出しは人間が行う前処理です）。
`run-request.json` の `sourcePath` は、このフォルダに置いたファイルを指すようにします。
