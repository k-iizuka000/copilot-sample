---
id: "{{機能ID}}"
generatedFrom:
  - "{{元設計書ファイル名1}}"
  - "{{元設計書ファイル名2}}"
generatedAt: "{{生成日時 ISO8601}}"
model: "{{GPT-5.4 | GPT-5.4 mini}}"
provisional: {{true|false}}
---

# {{機能ID}} 検索・更新要領

<!--
使い方: このテンプレートをコピーして spec/back/{機能ID}/queries.md を作る。
（バッチの queries.md も本テンプレートを流用する。id はジョブIDにする。）
載せるレコード: search_requirement / update_requirement / select_constraint / item_db_mapping（責務=Mapper XML / DAO）。
振り分け規則の正本: spec-format/routing-table.md。
出典列の書式: シート名!範囲 の短縮表記。
JSON に無い SQL・条件を創作しない。テーブルの物理定義（型・桁）は本ファイルに複製せず db/{テーブル物理名}.md へリンクする。
-->

## 1. 検索要領（search_requirement）

| 検索要領ID | 入力テーブル | 条件 | 出力項目 | 並び順 | 方向 | Record ID | 出典 |
|---|---|---|---|---|---|---|---|
| {{検索要領ID}} | {{入力テーブル}} | {{条件}} | {{出力項目}} | {{並び順}} | {{方向}} | {{RecordID}} | {{シート名!範囲}} |

## 2. 更新要領（update_requirement）

| 更新要領ID | 対象テーブル | 編集要領 | Record ID | 出典 |
|---|---|---|---|---|
| {{更新要領ID}} | {{対象テーブル}} | {{編集要領}} | {{RecordID}} | {{シート名!範囲}} |

## 3. 抽出制約（select_constraint）

| 制約ID | 対象 | 制約内容 | Record ID | 出典 |
|---|---|---|---|---|
| {{制約ID}} | {{対象}} | {{制約内容}} | {{RecordID}} | {{シート名!範囲}} |

## 4. 画面項目・DB項目対応（item_db_mapping）

| 画面項目番号 | 画面項目名 | Table物理名 | カラム名 | 特記事項 | Record ID | 出典 |
|---|---|---|---|---|---|---|
| {{画面項目番号}} | {{画面項目名}} | {{Table物理名}} | {{カラム名}} | {{特記事項}} | {{RecordID}} | {{シート名!範囲}} |

## 5. テーブル定義参照

<!-- 参照テーブルの物理定義（型・桁・PK等）は db/{テーブル物理名}.md が正本。ここではリンクのみ。 -->

| テーブル物理名 | 参照先 |
|---|---|
| {{テーブル物理名}} | [spec/db/{{テーブル物理名}}.md](../../db/{{テーブル物理名}}.md) |
