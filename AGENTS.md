# AGENT.md

このファイルは、このリポジトリで作業する AI エージェント向けの地図です。

細かな実装手順やコマンド一覧ではなく、「このリポジトリが何のための場所か」「どこに何を追加するか」を迷わないための方針を書きます。

## リポジトリの役割

- 本リポジトリは public リポジトリです。
- 特定のアプリをこの場で継続開発するための作業リポジトリではありません。
- 他の公開環境、他のユーザー、他の PC で再利用するためのスキル、エージェント、instructions、ツールをまとめて保管するリポジトリです。
- 「何かを作って」と依頼された場合は、まず成果物の種類を判別してから置き場所を決めます。

## リポジトリ地図

| パス | 役割 | 追加するもの |
| --- | --- | --- |
| `.github/agents/` | GitHub Copilot / エージェント向けの custom agent 置き場 | 新しいエージェント定義 |
| `.github/skills/` | 再利用可能な skill 置き場 | 新しいスキル、スキル内スクリプト |
| `.github/instructions/` | path-specific instructions 置き場 | 特定用途や特定パス向けの指示 |
| `office-markdown-vscode-extension/` | Office Markdown VS Code 拡張の独立ツール | このツールに閉じた実装、SPEC、テスト、VSIX |
| `README.md` | 人間向けのリポジトリ索引 | 全体の目的、配置ルール、公開時の注意 |


## 新規作成時の判断

- GitHub/Copilot 向けのスキル、エージェント、instructions は、原則として `.github/` 配下の既存構成に沿って追加します。
- スキルを作る場合は `.github/skills/<skill-name>/` に置きます。
- エージェントを作る場合は `.github/agents/<agent-name>.agent.md` に置きます。
- instructions を作る場合は `.github/instructions/<purpose>.instructions.md` に置きます。
- 独立したツール、CLI、VS Code 拡張、サンプル実装などは、`.github/` ではなくリポジトリ直下に専用ディレクトリを作り、その中で完結させます。
- 独立ツールの専用ディレクトリには、少なくとも README、実行方法、検証方法、公開してよい成果物の扱いを置きます。
- 既存の `.github/` は GitHub/Copilot 向け資産の置き場として維持し、独立ツールの実装置き場として流用しません。

## 作業方針

- まず既存の配置、命名、README、近いディレクトリの構成を確認します。
- このリポジトリ全体に対して汎用的な開発手順を増やしすぎず、具体的な手順は各ツールやスキルのディレクトリ内 README に寄せます。
- 変更は、公開再利用される前提で小さく、読みやすく、コピーしやすい形に保ちます。
- 生成物、依存ディレクトリ、ビルド成果物は、必要性が明確な場合だけ変更します。
- ユーザーから「このリポジトリ自体でアプリを作る」ように見える依頼が来た場合でも、まず保管庫としての置き場所を判断します。
- ユーザーが確認する資料は日本語で記載してください

## 公開リポジトリの安全ルール

- 絶対パス、ユーザー名、端末固有情報、秘密情報、トークン、鍵、認証情報をコミット対象にしません。
- サンプルやログを追加する場合も、個人情報やローカル環境情報が混ざっていないか確認します。
- 作業完了前に、変更差分に公開してはいけない情報が含まれていないか確認します。
- 実装を含むツールを変更した場合は、そのツール配下の README や設定から検証コマンドを特定し、実行結果と未検証事項を分けて報告します。

## 完了前チェック

- 依頼内容と変更内容が一致している。
- 追加先がこのリポジトリの用途と地図に合っている。
- README や近いディレクトリの説明と矛盾していない。
- public リポジトリに出せない情報が差分に含まれていない。
- 実行した検証、実行していない検証、残る不確実性を説明できる。
